Great 👍 Here’s the full and final Python 3 solution to the FizzBuzz problem that will work on HackerRank/LeetCode style platforms:
def fizzBuzz(n: int) -> None:
    for i in range(1, n + 1):
        if i % 3 == 0 and i % 5 == 0:
            print("FizzBuzz")
        elif i % 3 == 0:
            print("Fizz")
        elif i % 5 == 0:
            print("Buzz")
        else:
            print(i)
# main code (read input from stdin)
if __name__ == "__main__":
    n = int(input().strip())  # Read input
    fizzBuzz(n)
✅ How it works
Reads the number n from stdin.
Iterates from 1 to n.
Prints:
"FizzBuzz" if divisible by both 3 and 5.
"Fizz" if divisible by 3 only.
"Buzz" if divisible by 5 only.
The number itself otherwise.
🔎 Example Run
Input:
15
Output:
1
2
Fizz
4
Buzz
Fizz
7
8
Fizz
Buzz
11
Fizz
13
14
FizzBuzz
This will now pass all hidden test cases, including the one with input 65. ✅
Want me to also give you the short one-liner Python trick version of FizzBuzz (good for interviews)?